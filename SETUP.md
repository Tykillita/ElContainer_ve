# Setup

1. Clona el repo y entra a la carpeta.
2. Instala dependencias con tu gestor (npm/pnpm).
3. Copia `.env.example` a `.env` y completa valores.
4. Ejecuta `npm run dev` para desarrollo o `npm run build` para produccion.
5. (Opcional) Usa `supabase/` para scripts de base de datos.
