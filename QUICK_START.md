# Quick Start

1) Instala dependencias: `npm install` (o `pnpm install`).
2) Crea `.env` desde `.env.example` con tus claves.
3) Corre el entorno dev: `npm run dev`.
4) Edita `src/pages/Home.tsx` y empieza a agregar contenido real.
